# The main pipeline for spotify_bundle

resources:
  pipelines:
    spotify_bundle_etl:
      name: spotify_bundle_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/spotify_bundle_etl"

      libraries:
        - glob:
            include: ../src/spotify_bundle_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
